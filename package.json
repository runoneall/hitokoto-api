{
  "private": true,
  "name": "hitokoto",
  "version": "1.0.0",
  "description": "Hitokoto on Cloudflare Workers",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "runoneall",
  "license": "0BSD",
  "dependencies": {
    "wrangler": "^3.24.0"
  }
}
